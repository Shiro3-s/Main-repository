# Dependencies
/node_modules
/npm-debug.log*
/yarn-debug.log
# Production/Build files
/dist
/build
# Environment files
# Es Crucial ignorar el archivo .env ya que contiene secretos (JWT_SECRET, claves de DB, etc.)
.env
# Runtime data
/pids
/logs
*.pids
*.seed
*.log
# Editor/OS specific files (Archivos temporales del sistema o del editor)
.DS_Store
.vscode/
# Nodemon (si genera archivos temporales específicos, aunque generalmente no es necesario)
.nodemon
